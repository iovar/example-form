1. Complete recursive conversion of form to JSON
2. Try some more inputs
3. Read back values from JSON
4. Add a select that accepts options and renders correctly, also with multi, min/max, label renderer function

